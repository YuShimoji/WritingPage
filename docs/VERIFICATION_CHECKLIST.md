# Session 16 手動確認チェックリスト

> 対象: SP-079 (chapterMode UX) + DSL挿入GUI (Phase 1-2)
> 確認環境: `npm run dev` → localhost:8080

---

## 1. SP-079: chapterMode デフォルト化

### 1-1. 新規ドキュメント作成
- [ ] 「新規ドキュメント」を作成する
- [ ] Focusモードに切り替え → チャプターリストに「本文」が表示される (chapterMode が有効)
- [ ] 「章モードを解除」ボタンが表示されている (薄い表示)
- [ ] 移行ボタン「章モードに変換」は**表示されない** (既にchapterMode)

### 1-2. 文字数カウント
- [ ] テキストを入力し、ステータスバーの文字数が表示される
- [ ] DSL構文 (`:::zw-typing{speed:"50ms"}\nテスト\n:::`) を入力した場合、文字数がDSL記法を**含まない** (「テスト」2文字のみ)
- [ ] チャプターリストの文字数表示もDSLを除外した値

### 1-3. ロールバック
- [ ] Focusモードで「章モードを解除」ボタンをクリック
- [ ] 確認ダイアログが表示される
- [ ] 承認すると通常テキストモードに戻る (全章が結合される)
- [ ] 再度「章モードに変換」ボタンが表示される

### 1-4. Legacy 変換バナー
- [ ] 旧フォーマットのドキュメント (既存データがある場合) を開くと、エディタ上部に青いバナーが表示される
- [ ] バナーの「新フォーマットに変換」ボタンで変換できる
- [ ] バナーの「x」ボタンで閉じられる
- [ ] chapterMode のドキュメントではバナーが**表示されない**

---

## 2. DSL挿入GUI

### 2-1. TBドロップダウン
- [ ] WYSIWYGモードに切り替え
- [ ] WYSIWYGツールバーの「TB」ドロップダウンを開く
- [ ] 以下の4つの演出ボタンが表示される:
  - [ ] タイピング演出を挿入
  - [ ] ダイアログを挿入
  - [ ] スクロール演出を挿入
  - [ ] パステキストを挿入

### 2-2. 属性設定モーダル
- [ ] 「タイピング演出を挿入」クリック → モーダルが開く
  - [ ] 速度 (30ms/50ms/80ms/120ms) 選択可能
  - [ ] モード (auto/click/scroll) 選択可能
  - [ ] 「挿入」で確定 → エディタにタイピングブロックが挿入される
  - [ ] 「キャンセル」でモーダルが閉じる
- [ ] 「ダイアログを挿入」クリック → モーダルが開く
  - [ ] 話者名テキスト入力
  - [ ] 位置 (left/right/center) 選択
  - [ ] スタイル (default/dark/neon/retro) 選択
- [ ] 「スクロール演出を挿入」クリック → モーダルが開く
  - [ ] エフェクト (fade-in/slide-in/slide-up) 選択
  - [ ] 遅延 (ms) テキスト入力
  - [ ] 閾値 テキスト入力
- [ ] 「パステキストを挿入」クリック → モーダルが開く
  - [ ] パス形状 (波形/S字カーブ/直線/円弧) 選択

### 2-3. 挿入結果確認
- [ ] 挿入された各ブロックが WYSIWYG エディタ内に表示される
- [ ] Readerプレビューモードに切り替え → 挿入した演出が正しくレンダリングされる
- [ ] テキストモードに戻す → DSL構文として正しくシリアライズされている

---

## 3. 回帰確認

- [ ] 既存のテキスト入力/保存が正常に動作する
- [ ] テーマ切替 (ライト/ダーク) でモーダルとバナーの表示が崩れない
- [ ] Focus/Normal/Blank モード切替が正常
- [ ] ファイルの切り替え (ドロップダウン) が正常
