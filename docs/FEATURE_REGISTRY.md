# Feature Registry（機能台帳）

Zen Writer の**ユーザー向け機能**を一覧し、仕様の正本・実装の所在・テストの有無を追跡する。詳細仕様は各 `docs/specs/*.md` を正とする。

## 運用

- 新規ユーザー機能・ガジェット・モードを追加したら、下表に 1 行追加する
- 「テスト」列は E2E spec ファイル名（`e2e/*.spec.js`）または `manual` / `none`
- 大きな変更が入ったら「最終確認日」を更新する

## レジストリ

| ID | 機能名 | 概要 | 仕様 / 正本 | 主な実装 | テスト | 最終確認日 |
|----|--------|------|-------------|----------|--------|------------|
| FR-001 | 統合シェル状態 | 公開 UI は `display mode` ではなく、command palette / left nav root-category / replay surface で扱う。`normal` / `focus` は内部互換 API 値。Reader はモードではなく再生オーバーレイ導線（`data-reader-overlay-open`） | `docs/INTERACTION_NOTES.md`, `docs/INVARIANTS.md`, `docs/specs/spec-ui-mode-sidebar-residuals.md` | `js/app.js` (`setUIMode`), `js/reader-preview.js`, `js/sidebar-manager.js`, `js/edge-hover.js`, `js/top-chrome-controller.js`（互換 shim） | `ui-mode-consistency.spec.js`, `reader-preview.spec.js` | 2026-04-28 |
| FR-002 | コマンドパレット | 横断コマンド実行。visible shell command は command palette / left nav / Reader surface の操作名を基準にし、旧 `ui-mode-*` / top chrome 表示導線を主語にしない | — | `js/command-palette.js` | `command-palette.spec.js` | 2026-04-28 |
| FR-003 | MD→HTML 後処理パイプライン | プレビューと Reader で同一順序の装飾・章リンク変換。markdown-it 前段は `ZWMdItBody` で共有。監査台帳 `docs/WP004_PHASE3_PARITY_AUDIT.md`（複数見出し+chapter://・壊れ wikilink・zw-textbox 複合・zw-typing/dialog+ルビ・ジャンルプリセット浅い E2E・Reader 壊れ wikilink ポップオーバー・Reader 章末ナビ結合 smoke・**段落 typography CSS 変数の preview/Reader 一致（session 66）** 等） | `docs/INTERACTION_NOTES.md`（WP-004）、`docs/specs/spec-textbox-render-targets.md` | `js/zw-markdown-it-body.js`, `js/zw-postmarkdown-html-pipeline.js`, `js/zw-inline-html-postmarkdown.js`, `css/style.css`（プレビュー／Reader 本文の段落・字下げ規則） | `reader-wysiwyg-distinction.spec.js`, `reader-genre-preset.spec.js`, `reader-wikilink-popover.spec.js`, `reader-chapter-nav.spec.js` | 2026-04-07 |
| FR-004 | 章リンク変換 | `chapter://` → `.chapter-link`、Reader/エクスポート用 `#` 化 | SP-072 系 | `js/chapter-nav.js` | `reader-wysiwyg-distinction.spec.js` | 2026-04-06 |
| FR-005 | ツールバー／執筆域幾何 | `--toolbar-height` 同期、Focus 上端ホバー時の余白 | — | `js/app.js`, `css/style.css` | `toolbar-editor-geometry.spec.js` | 2026-04-06 |
| FR-006 | 段落の横方向揃え（P2） | リッチ編集表示（`editor-wysiwyg.js`）で `data-zw-align`、コマンドパレット・「その他」メニュー、paste、Turndown。CommandAdapter 操作後は Markdown 正本へ同期し、保存・MD preview 更新まで行う。**MD プレビュー（`#markdown-preview-panel`）と読者本文（`.reader-preview__content`）**は `css/style.css` で投影し、`data-zw-align` は save/reload 後も保持する。改行: `effectBreakAtNewline` と `effectPersistDecorAcrossNewline` はショートカット（後者のみ）に加え **UI Settings**（詳細設定）からも設定可（session 60–61） | `docs/specs/spec-rich-text-paragraph-alignment.md`, `docs/specs/spec-rich-text-newline-effect.md`, `docs/verification/2026-06-25/rich-text-block-align-persistence.md` | `js/modules/editor/RichTextCommandAdapter.js`, `js/modules/editor/RichTextEnhancedRuntime.js`, `js/zw-markdown-it-body.js`, `js/editor-wysiwyg.js`, `js/command-palette.js`, `js/gadgets-editor-extras.js`, `index.html`, `css/style.css` | `rich-text-block-align.spec.js`, `reader-wysiwyg-distinction.spec.js`, `editor-settings.spec.js` | 2026-06-25 |
| FR-007 | WYSIWYG カスタム Undo 粒度 | `Ctrl+Z` / `Ctrl+Shift+Z` 用スタック。連続入力はデバウンスバッチに加え、**Space / Enter / blur / IME compositionend** で保留バッチをフラッシュし Undo 単位を区切る（session 62）。**Undo はスタックを 1 件だけ pop**（session 64）。**Redo 用スナップショットは Undo 直前の実 DOM `innerHTML`**（session 65）。E2E: Space・Enter・blur・Undo 後 Redo・**連続 Undo/Redo（Space 区切りセグメント）**・**Space 後の長い連続入力を 1 段 Undo**（別レーン session） | `docs/specs/spec-richtext-enhancement.md`（Phase 4） | `js/editor-wysiwyg.js` | `wysiwyg-editor.spec.js`（FR-007） | 2026-04-07 |
| FR-008 | WYSIWYG タイプライター短文アンカー | タイプライター ON 時、`paddingBottom` に加え **`paddingTop: calc(100vh * (1 - anchorRatio))`** で上方向スクロール域を確保。`_scrollCursorToAnchor` の `scrollTop` をクランプ（session 63）。E2E: `paddingTop` の有無に加え、**長大本文で input 後の `scrollTop` が `[0, maxScroll]` に収まる**（別レーン session） | `docs/specs/spec-richtext-enhancement.md`（Phase 4） | `js/editor-wysiwyg.js` | `wysiwyg-editor.spec.js` | 2026-04-07 |
| FR-009 | アプリ内ヘルプ資源（SSOT: EDITOR_HELP.md） | `docs/EDITOR_HELP.md` を SSOT とする UI 説明の集約先。active help / shortcut descriptions は `F1 = help`、`F2 = command palette`、公開 UI は command palette / left nav / Reader surface / Local Gadget で説明する。旧 `Normal / Focus / 表示モード切替` 語彙と旧 Focus パネル由来の設定入口は現行案内から外す。ヘルプ入口はコマンドパレット (`open-help`) と `F1`、設定入口は `Ctrl+,` とコマンドパレット (`open-settings`) に限定する | `docs/EDITOR_HELP.md`（SSOT）, `docs/GADGETS.md`, `docs/specs/spec-local-gadget-mods.md` | `js/gadgets-help.js`（`#help-modal` の表示器）, `js/gadgets-markdown-ref.js`（shortcut descriptions）, `index.html`（`#help-modal` / `#settings-modal` 本体は維持）, `js/keybind-editor.js` (`app.help.open` / `app.settings.open`), `js/app-shortcuts.js` | `command-palette.spec.js`, `keybinds.spec.js`, `visual-audit.spec.js` | 2026-05-10 |
| FR-010 | 浮遊メモ実験（隔離 overlay） | 既存 app に dev-only / experimental な独立 overlay を追加し、**DOM + CSS 3D + 軽量 JS state** で複数メモの **通過フロー + respawn**、**背景面ドラッグ / foreground 編集昇格**、**release 後の inertia return + paper flutter** を検証する。背景 memo は visible title / state / `DRAG` / textarea 枠を持たない read-only fragment、foreground だけ borderless textarea として編集する。touch / coarse pointer では **1 本指即ドラッグ + 8px slop**、**背景 tap で foreground 化 / 次の tap で編集**、**2 本指 gesture 無効**、**visualViewport ベースのキーボード回避** を追加する。ユーザー向け入口は command palette の `浮遊メモ実験` のみで、説明は「保存されない隔離実験 overlay」。`?memoLab=1` は E2E / developer 用の直接起動 hook。開く時は Reader / command palette を畳み、閉じると編集面へ focus 復帰する。mock dataset + localStorage は実験内部状態として扱い、editor / chapter / autosave 本流、正式 Gadget、loadout へ侵入しない | `docs/CURRENT_STATE.md`（隔離サイドクエスト行）, `docs/USER_REQUEST_LEDGER.md`, `docs/ROADMAP.md` | `js/floating-memo-field.js`, `index.html`, `css/style.css`, `js/app.js`, `js/command-palette.js` | `floating-memo-lab.spec.js`, `daily-writing-proof.spec.js`, `command-palette.spec.js` | 2026-05-06 |
| FR-011 | 統合シェル UI | 公開 UI から `display mode` を外し、**command palette / left nav hierarchy / replay surface** を基底にした単一シェルへ再編。visible top chrome surface は廃止し、F2 / Electron menu / 旧 toolbar 互換経路は command palette を開く。left nav は root/category 階層式で、active category を左上固定し、root では **last active cue** を残す。command palette と visible shell wording から `ui-mode-*` / `toggle-fullscreen` / top chrome 表示導線を外す。legacy stored UI values は互換値として `normal` に吸収する。 | `docs/INTERACTION_NOTES.md`, `docs/CURRENT_STATE.md`, `docs/INVARIANTS.md` | `index.html`, `css/style.css`, `js/top-chrome-controller.js`, `js/sidebar-manager.js`, `js/app.js`, `js/app-shortcuts.js`, `js/command-palette.js`, `js/electron-bridge.js`, `js/keybind-editor.js`, `js/storage.js`, `js/edge-hover.js`, `electron/main.js` | `ui-mode-consistency.spec.js`, `command-palette.spec.js`, `accessibility.spec.js`, `responsive-ui.spec.js` | 2026-04-28 |
| FR-012 | Writing status chip | Reader 非表示 / Floating memo lab 非表示の通常執筆時だけ、非操作型 chip で `文字数: N · 編集中/保存済み HH:mm` を表示する。保存失敗時は `data-save-state="failed"` と `保存失敗` を表示し、input だけでは楽観的に保存済みへ戻さない。`data-last-saved-at` は最後に保存済みへ遷移した ISO 時刻を保持する。Reader / Floating memo lab 表示中は隠し、常用 toolbar や drag region は増やさない。 | `docs/INVARIANTS.md`, `docs/UI_SURFACE_AND_CONTROLS.md`, `docs/EDITOR_TRUST_WORKFLOW.md` | `js/writing-status-chip.js`, `index.html`, `css/style.css` | `accessibility.spec.js`, `ui-mode-consistency.spec.js`, `daily-writing-proof.spec.js`, `editor-trust-workflow.spec.js` | 2026-06-15 |
| FR-013 | Local Gadget Mod | ガジェットを固定ラックではなく、後から着脱できる Trusted local Mod として扱う。`js/plugins/<mod-id>/index.js` を `js/plugins/manifest.json` に登録し、設定モーダルの `ローカルMod` で enable 状態を保存する。enable 済み gadget Mod は `source: 'plugin'` / `pluginId` を持つ gadget として指定 group に表示され、必要に応じて `api.gadgets.registerSettings()` で settings UI も同じ Mod 境界に登録できる。2026-05-10 時点の migration set は `MarkdownPreview` / `HUDSettings` / `PomodoroTimer` の 3 件で closeout 済み。 | `docs/specs/spec-local-gadget-mods.md`, `docs/PLUGIN_GUIDE.md`, `docs/GADGETS.md`, `docs/INVARIANTS.md`, `docs/verification/2026-05-10/local-gadget-mod-boundary-closeout.md` | `js/plugin-manager.js`, `js/plugin-api.js`, `js/gadgets-plugin-manager.js`, `js/gadgets-core.js`, `js/plugins/manifest.json`, `js/plugins/sample-word-count-gadget/index.js`, `js/plugins/markdown-preview-gadget/index.js`, `js/plugins/hud-settings-gadget/index.js`, `js/plugins/pomodoro-timer-gadget/index.js`, `index.html`, `css/style.css` | `plugin-manager.spec.js`, `gadgets.spec.js`, `pomodoro.spec.js` | 2026-05-10 |
| FR-014 | Rich editing typed heading shortcut | Rich editing 通常入力で、行頭 `# ` / `## ` / `### ` を Space で確定した時だけ H1/H2/H3 へ変換する限定 shortcut。IME composition 中、`#hashtag`、行中 `# `、`#### `、paste、import、Markdown source round-trip、汎用 Markdown shortcut は対象外。Undo は変換直後 1 回で typed marker へ戻れることを守る。 | `docs/CURRENT_STATE.md`, `docs/USER_REQUEST_LEDGER.md`, `docs/verification/2026-06-08/rich-editing-heading-shortcut-handoff.md` | `js/editor-wysiwyg.js` | `wysiwyg-editor.spec.js`（heading shortcut） | 2026-06-08 |

## 未登録の既存機能

既存コードベースの機能は随時この表へ移行する。当面は `docs/spec-index.json` および `docs/FEATURE_REGISTRY.md` の両方を参照し、重複記述を減らす方向で統合する。

## 関連

- `docs/AUTOMATION_BOUNDARY.md` — E2E と手動検証の境界
- `docs/ROADMAP.md` — 優先度とロードマップ
- `docs/CURRENT_STATE.md` — セッション単位のスナップショット
