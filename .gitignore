# OS / Editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Build outputs (not used now but reserved)
dist/
build/
# 退避用出力ディレクトリ (session 98-99: build/ ロック時の workaround)
build-new/
# セッション別退避用 (session 103: build/ + build-new/ 同時ロック時の追加 workaround)
build-session*/
# Packaged verification workaround output (when build/ is locked)
build-friction/

# Node
node_modules/

# Vendor fonts (Electron-only, generated by postinstall)
vendor/fonts/
# Kuromoji dictionary (17MB, generated by postinstall)
vendor/kuromoji-dict/

# Temporary
.tmp/
.cache/
tmp/

# Local helper scripts
scripts/gh-*.ps1

# DocFX build outputs
_site/
.docfx/

# Playwright outputs
test-results/
playwright-report/
.tmp-playwright-libs/
debug-screenshot-*.png
debug-*.md
debug-*.html
temp-*.html
quick-fix*

# Temporary test output files
*-out.txt
*-out?.txt
*-fail.txt
test-*.txt

# Playwright screenshot captures
output/

# Submodule remnants (removed 2026-03-08, re-removed 2026-03-11)
.shared-workflows/
shared-workflows/
.gitmodules

# Windows artifacts
nul
*:Zone.Identifier

# Tool-specific / AI generated
.claude/
.serena/
test-artifacts/
