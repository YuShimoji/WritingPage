name: CI Smoke

on:
  push:
    branches:
      - main
      - develop
      - 'feat/**'
  pull_request:
  workflow_dispatch:

jobs:
  smoke:
    uses: YuShimoji/shared-workflows/.github/workflows/ci-smoke.yml@v2.0
    secrets: inherit
